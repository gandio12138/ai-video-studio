# Harness v2.3｜双入口＋主动补全＋视觉交付＋后期

生图提示词共同必读PROMPT_AUTHORING_STANDARD.md：统筹→补全→视觉→提示词→审查→图像执行均沿用同一写作与验收依据。直接提出人物、全身、母图、场景、道具、单镜或编辑任务时同样适用，不必重走整集；样例只传递方法，不提供新对象的身份或换装授权。

```text
用户原文/创意 + 已确认约束
              ↓
manju-producer 读取CINEMATIC_LOOK，选择内容入口与full_pack/calibrate_only范围
  A 小说 → source_packet/台词（不独立打磨剧本）
  B 设定/人物/概述 → 简报/首集剧本/台词
              ↓
manju-enrich：服饰/外貌/场景/道具/局部事件接续，CMP台账
              ↓
manju-visual ⇄ manju-shots：电影式详细设计、固定布局/灯位与可接续的单幅镜头草稿
              ↓
manju-prompts：必要身份/衣装/空景参考 + 道具/每镜/编辑完整正文 + 覆盖索引
              ↓
manju-audit：来源、人物适配、因果、时长与scope内逐资产/逐镜文字视觉检查
              ↓
G1内容/台词/叙事补充；G2视觉/衣装/场景/声音身份
              ↓
明确授权的定妆小样/试音 → 采用参考/实测配音/关键帧
              ↓
有声分镜小样 + G3批量预算 → 动态镜头或静帧
              ↓
中立timeline → G4粗剪/定剪 → 字幕/混音 → G5交付确认
```

上图是协作顺序，不是自动执行引擎。原版所有媒体接口仍需真实接入；本次增加的是文字补全技能、标准和模板。定妆与声音的实际小样可分批授权，不要求为了看草稿先买云端套餐。

## 文本工作和执行分开
两个v2.3入口均允许G1前本集的文字试写/试拆、视觉设计和提示词草稿。只有原文不可读/片段未定/核心输入确实缺失等问题才暂停对应内容。缺参考、API或设计批准不阻塞条件式文字稿，却阻塞未获授权的生成。完整入口不能在script/shots之后提前宣布完成。

plan写草稿/离线检查；preview需授权本地低分辨率粗剪；production需服务、输入上传、预算和重试范围。运行模式不是系统沙箱；FFmpeg后端的execute开关不约束所有其他Codex工具。

## 一套来源与依赖
事实与用户给定保留；CMP补充台账→同版视觉卡/内容→镜头/提示词→真实参考/声音/视频→时间线。普通A/B补充主动提出；C核心变化按入口处理，不能由美术擅自改关系或线索。

改衣装只影响对应视觉；改对白影响对应配音/对齐和可能的镜长；改空间结构影响相关场景和反角；补动作需重新合计时长。不要全量重做，也不要后读原文后强行忽略设计冲突。

## 状态与媒体恢复
文字draft/prompt_only、参考awaiting_reference、实际资产planned/generated/approved是不同维度。任务成功只代表真实文件落地并核验，不等于用户质量批准。

保留job_id、版本、文件哈希与来源。超时先查询/核账，未知状态needs_reconciliation，不直接重建付费任务。缓存只复用输入/模型/参数/参考和输出核验一致的结果。

## 后期仍沿用原协议
TTS区分spoken_text/direction/subtitle_text；实测时长回写镜头，跨镜一句声音不重复计时。口型是独立能力。timeline用整数帧，源素材入点用毫秒；不是剪映/FinalCut/Resolve原生工程。当前后端具体支持与验证边界见CAPABILITIES.md、contracts/PRODUCTION.md，不静默忽略不支持效果。

文字审美检查与实图目检不同；没看/听过就写not_checked。不自动发布或承诺后台完成。

## 可选角色/场景设定板分支（仅显式要求时）
当前cinematic_live_action按CINEMATIC_LOOK.md；两完整入口和complete_visual_pack保持full_pack，风格纠偏rebuild_cinematic_visuals默认calibrate_only。只维护/初始化不执行创作，合并报告后停止。
以下设定板分支只在用户启用时执行，不作为默认电影式交付门槛。
visual锁人物/衣装/场景布局 → prompts按请求交完整整板与适用独立分格正文 → 明确小样许可 → images按本批选择“采用主参考直接出整板候选并逐格检查”或“独立图派生/逐图复核后仅排版拼板”。不以尚无全部侧背图阻塞整板文本，不无故重做已有合格全身主图。
板用于人工审设计，独立图用于合成镜头，镜头用于视频。读DESIGN_SHEETS.md，避免把三者混为一种资产。文本先全覆盖，实际生成分批；本次未增加图片生成/拼版代码。
