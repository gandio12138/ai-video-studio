# Harness v2.1：双入口 + Skill + 状态 + 数据协议 + 工具后端 + 验收

## 架构
```text
用户批准 / AGENTS.md / PROJECT.md
                 ↓
manju-producer（当前阶段、预算、依赖、恢复）
                 ↓
A 小说 → 原文制作依据/台词 → 分镜试拆/保真检查
B 设定/人物/概述 → 首集剧本/台词/审校 → 分镜
                 （G1 内容依据与台词确认）
                          ↙           ↘
              声音定妆/逐句配音        视觉定妆/关键帧
                         ↘           ↙
                    有声分镜低成本小样
                           ↓
              逐镜视频或静帧制作 → 实际素材验收
                           ↓
          timeline.json → 粗剪 → 定剪 → 字幕/混音复核
                           ↓
                   真实导出 → QC → 人工发布确认
```

## 三层不要混淆
**技能层**教 agent 怎么决策和交接。**数据层**保存资产、台词、任务、时间线、确认与依赖。**工具层**才真正发请求、测媒体、生成声音、渲染视频。
只加 SKILL.md 并不会获得配音账号、视频模型或剪映控制能力。本版已实现小样剪辑后端，云端生成仍是契约与任务预留。

## 权限与阶段
plan 可写草稿和做离线检查；preview 需用户授权本地渲染；production 需服务、上传资料、预算、重试范围、输出位置都明确。执行模式是协作约定，CLI 的 `render` 默认 dry-run，只有 `--execute` 才真的渲染；其他 Codex 工具不受本包强制管辖。
G1 锁内容依据与采用台词：A 锁原文范围/source_packet/补充，B 锁原创剧本；G2 锁声音/视觉身份；G3 确认小样和批量预算；G4 确认粗剪与定剪；G5 确认最终交付/发布。

## 依赖不是单向死流水线
先有估算分镜，再以真实配音长度修订。声音、关键帧可并行；优先小样后再付费做大量动态镜头。精剪可能需要重新配一句或延长一个镜头，不能以“下一关已经完成”拒绝合理返工。

## 失败与缓存
逐任务状态：planned → awaiting_approval → ready → submitted → running → succeeded；可转 failed/cancelled/needs_reconciliation/stale。人工导入经过核验后也可 succeeded，但 provider=manual_import，不能伪造远端任务。
远端成功只是传输/媒体成功，review_status 仍可未审/拒绝。网络超时先查已有 job_id 或人工核账；未知时暂停，不直接再生成。重试次数包含首次；所有重试共用已批准费用范围。
输入、模型、参数、参考图/声音卡哈希参与 request_fingerprint；换一句话、新 take、换模型或图，都要变化。已批准文件不覆盖；修改采用指针和受影响的下游状态。没有输入哈希的一般草案不能作为可靠缓存键。

## 时间线与声音
时间线视频轨采用整数帧，fps 用有理数。素材裁切 source_in_ms 是素材自己的毫秒时间，不是时间线帧号。音轨开始与长度也在时间线帧网格上；句子真实持续时间另由音频实测保留。
台词可跨画面切换；不要将同一句按镜头复制到多个音轨。先定对白节奏，再精确字幕。字幕时间基于实际采用音轨，估时仅用于草案。

## 开工方式
先按用户要求让 producer 检查或读取固定入口：A 用 prompts/entry_novel_direct.md，B 用 prompts/entry_original_script.md。详细路由见 WORKFLOW_ENTRYPOINTS.md；A 不额外打磨剧本，B 不要求小说。需要配音时才选一个供应商和两三个试音，不同时搭多个厂商集成。已有网页生成文件可先手动导入，这不会阻塞本地剪辑。
技术格式的详细字段见 contracts/PRODUCTION.md。工具与真实能力以 CAPABILITIES.md 为准。
