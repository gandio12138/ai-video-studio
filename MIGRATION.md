# v1 → v2：不要覆盖你的创作

## 还没写正式内容
保留旧文件夹作备份，另解压 v2，在新目录启动 Codex。旧 examples 不需要当作正式项目搬入。

## 已经有小说、大纲、剧本或素材
先做整个旧项目的备份。将 v2 放在平行目录，不要直接覆盖解压。
让 Codex 比较两边（只授权这两个目录），先出迁移计划。确认后逐步合并：
- `.agents/skills`：新增八个；旧六个合并“v2 生产交接”段落，保留你自己的要求。
- `AGENTS.md`：以 v2 新范围为参考，保留已有个性化规则和确认约定。不要同时保留“永远只做文字”与“允许用户授权后剪辑”的冲突指令。
- `PROJECT.md` / `STATE.md`：保留你的内容与状态，新增声音/后期字段和阶段，不能把已完成工作重置为未开始。
- `config` / `contracts` / `tools` / `templates`：没有重名则新增，重名先 diff。
- `00_source`–`07_reviews` 与已有素材：不覆盖、不删、不替换为示例。
- 新增 `08_audio`–`12_runs`。这些目录不是你已有素材的迁移目标，除非明确需要。

旧 v1 shots.json 仍可由 check_shots.py 检查。v2 timeline 是新增编辑层，不强制改掉原文字镜头表。稳定 line_id 可逐步加入剧本/镜头对白对象。
迁移完成后重新运行 doctor，启动新一轮 Codex，只检查不生图/配音/渲染。没有写入实际用户批准，付费执行默认仍未授权。
