# 可直接复制的中文提示词库

这是本次定制的方法库，不是官方内置命令，也没有调用任何生图服务。`{花括号}` 表示需要替换的输入；可以直接告诉 AI 参数，让它帮你填。没有提供的原文、参考图、工具能力不能假装存在。

在 ChatGPT 中粘贴某一段并提供对应资料即可；本地路径只有在工具确实能访问时才有意义。在 Codex 中优先使用同名 manju-* skill，避免每次粘贴整本库。默认分批推进；两个入口明确允许的首轮文字批次可包含多个步骤，所有试写/试拆为 draft，不能自动越过确认关卡。

## 优先选择两个固定入口（v2.1）

- **有小说，不打磨剧本**：读取 `prompts/entry_novel_direct.md`，提供实际原文和本轮片段。跳过独立编剧/对白润色，输出制作依据、逐句台词、文字分镜试拆和保真检查。
- **没有小说，从剧本开始**：读取 `prompts/entry_original_script.md`，提供设定、人物、概述。首轮简报/大纲/首集试写/审校，不先写小说正文。

按 `WORKFLOW_ENTRYPOINTS.md` 解释后续模板。以下旧“写小说”和“创作性改编”模板只在用户明确要求时使用，不是两个默认入口的前置条件。G1 在 A 指 source_packet/原文范围/台词，在 B 指原创剧本/台词。

## 00｜初始化检查（在 Codex 交互输入框粘贴）

```text
请读取当前目录的 AGENTS.md、PROJECT.md、STATE.md，并检查 .agents/skills 下的十四个 manju-* 技能。
本轮只做初始化，不写故事、不生成图片、不联网安装插件、不改全局配置、不覆盖任何已存在的创作文件。
请列出：已读文件、十四个技能及分工、缺失输入、建议的第一步。
只新增 07_reviews/setup_check_v01.md；已有同名文件时递增版本。
不得把 examples 作为正式资料，不把缺小说作为所有入口的阻塞。
入口未指定时先问：有小说按原文制作，还是无小说直接开发剧本？已指定则不重复问。
```

## 01｜可选：原创小说立项与大纲（仅明确要求写小说时）

```text
请作为我的小说策划与漫剧改编编辑。
入口：原创。题材：{题材}。观众：{观众}。第一条试作：{秒数}秒。
制作限制：{角色数量}名主要角色、{场景数量}个主要场景，预算有限。
我的核心想法：{一句话想法；没有则说明暂无}。

先提出最多三个方案，不直接写长篇正文。每个方案包含：
一句话故事；主角的长期目标与本集可观察目标；阻力与失败代价；
主角主动采取的行动；核心选择；开端—对抗—转折—结果的因果链；
一句吸引人但不夸大的开场；第一集结束状态；新增素材与制作难点。

我选定后，再给人物表、世界规则、章节/分集大纲和伏笔表。
不要用“勇敢、腹黑、很帅”等标签代替动机，不依靠反派突然降智推动剧情。
所有未获确认的内容标为提案；不要假定我已批准。
```

## 02｜可选：已有小说的创作性改编（不是 novel_direct 默认步骤）

```text
只依据我提供的小说片段做拆解，不凭记忆补全同名作品。
片段：{粘贴正文或实际可读文件}。
目标：{题材/观众/时长/制作限制}。

先声明实际读到的范围，并输出：
1. 事件顺序与核心因果链，附真实章节/段落位置；
2. 人物身份、关系、长期目标、本集目标、阻力、代价、行动；
3. 观众与每个人物分别知道什么，在哪里得知；
4. 世界规则、已有伏笔、未解问题；
5. 适合第一条短片的一个冲突单元，说明开始和结束位置。

逐项区分【原文事实】【合理推断】【创作补充】【待确认】。
提出保留、删减、合并和新增建议，但不直接改掉核心身份、动机或因果。
输出适合人工确认的大纲，不要马上写分镜或生图提示词。
原文缺失时请索要，不伪造原文引句或定位。
```

## 03｜场景剧本与对白压缩（original_script / 用户单独批准的改写）

```text
依据我确认的大纲和人物表，把{指定单元}改成{目标时长}秒的场景剧本。
当前采用版本：{版本}。原文与大纲：{附件/正文}。

每场写：场次、地点、昼夜、双方目标、可见动作、对白、旁白、声音、
新信息、转折、离场状态。心理描写优先用动作和反应表达；不能等价转换时说明改编取舍。

对白请给“原句—保真压缩版—更口语版—不可丢信息—修改理由—丢失风险”。
删除重复解释和空话，但保留否定、时间、条件、关键线索与关系信息。
保持每个人的语气区别，不要人人都说同一种短视频腔。
压缩不是固定砍掉一半；信息必须完整。

按我给的语速假设粗估对白、停顿和动作时间，明确标“待朗读/录音验证”。
超过时长先给删减/重排建议，不为了塞入镜头让台词不可自然说出口。
不要自行生成全季，完成本单元后停下等确认。
```

## 04｜独立剧情与连续性审校（manju-audit）

```text
你现在只做审校，不夸奖，也不擅自重写全稿。
请检查这些具体版本：{原文、大纲、人物表、剧本或镜头表}。

检查：人物目标与行动是否一致；信息从哪里获得；因果是否断裂；
时间与地点是否矛盾；道具的出现、持有人和状态；伏笔与回收；
身份/世界规则是否漂移；重要信息有没有被对白压缩丢掉；
若有镜头表，再查动作、景别、视线与时长是否匹配。

每个问题给：编号、严重度、具体位置、原文/上游证据、影响、最小修复方案。
有意悬念不等于漏洞；无法证实的问题标“待证实”。
先给必须修的少量问题，再给可选优化，并声明未读范围。
不要声称“全书无漏洞”，除非实际覆盖了全部相关材料，而且仍需保留判断边界。
```

## 05｜剧本拆成文字镜头表（manju-shots）

```text
把采用内容依据{A 的 source_packet 或 B 的剧本版本}拆成一条{目标秒数}秒的文字分镜。未经 G1 确认只按明确授权试拆，标 draft。A 不要求另外写剧本。
画幅：{比例}。制作方式：静帧运镜优先，复杂动态镜头需给替代方案。

每镜填写：镜号、场次、原文/剧本节拍定位、叙事目的、景别、机位、
运镜或后期裁切、主体动作、对白、旁白、音效、剪辑采用秒数、
计划生成秒数、角色/场景/道具/声音素材编号、起止连续性、制作方式、后期备注。
每镜一个主要叙事任务，动作写成能看见的主体—行为—结果，
不能在同一个短镜头里完成多个地点和时间的变化。

区分场次 SC01 和可复用地点 L001。
区分采用时长与生成时长；没选视频模型的生成时长填 null，不编造支持档位。
请先合计所有采用时长，再检查对白与动作能否容纳。
重要纸条或屏幕文字另列后期准确文本。

输出易读镜头表及同版本 JSON。缺少资产则登记为 planned，不伪装已有图片。
如在本地项目中，按 templates/shot_fields.md 并运行可用的本地校验脚本；
没有实际执行就写明“未执行”，不能声称已通过。
```

## 06｜角色定妆与参考图计划（manju-visual / manju-prompts）

```text
依据人物表为{角色编号与名字}制作文字定妆卡和参考图任务单，不立即生图。
原文明确外貌：{内容}。允许创作补充：{范围}。统一画风：{风格}。

分开三类：
固定身份锚点（年龄观感、脸型、眼型、瞳色、发型发色、体型与标记）；
衣装版本（材质、颜色、饰品与角色自身左右）；
允许变量（姿势、表情、视线、景别）。
未在原文明确的部分标【创作补充/待确认】。

先规划一张正面半身主参考，确认后再由该参考派生全身、侧面、背面与常用表情。
每张都写需要上传哪张图、只允许改变什么、什么必须保留。
编号是文件管理标签，不把“同一个 C001”当成已上传人物参考。
不要保证百分之百锁脸，给出人工检查清单。
```

### 06A｜角色主定妆：直接给生图工具的自然语言草案

```text
为原创角色{姓名}制作一张单人主定妆参考图。
{已经确认的年龄观感、脸型、眼型、瞳色、发型发色、体型、独特标记}。
穿着{完整衣装与材质，包含配饰位置}。
正面半身，站姿自然，表情中性，双肩和发型完整可见，浅色简洁背景，柔和均匀照明。
风格：{明确的线条、色彩、阴影、比例和材质要求}。
仅一个人；不要拼成多人设定表；不加入未指定首饰、文字或水印。
这张图用于确定身份，不需要戏剧性运镜或复杂场景。
```

### 06B｜从已确认参考派生新角度

```text
附件图1是已经确认的角色身份和服装主参考。
请基于这张图制作{侧面/背面/全身/指定表情}参考。
保持脸部特征、发型发色、年龄观感、体型比例、服装剪裁和配饰位置一致。
只改变{本次允许的角度/表情/姿势}。不要重新设计角色。
保持简洁背景与均匀照明，输出一张单角色图片。
```

## 07｜场景图与场景构图（manju-visual / manju-prompts）

```text
为场景{L001/名称}建立可重复使用的场景设定，而不是一张随意背景。
原文事实：{地点、时间、关键家具/入口}。用途：{剧情用途}。

先用文字固定门、窗、桌子、通道和主要光源的相对位置，给简单平面关系。
区分不能变的空间结构和可以变的昼夜/灯光/移动道具。
规划一个空场景主视角与必要反角，说明哪些镜头会用哪个角度。
写一段空景生成提示词：景别、机位、前中后景、光源、材质、风格、画幅。
先不放人物，给后续角色留出站位和动作空间。
平台未定时不要编造固定的字幕安全区像素值。
```

### 07A｜空场景可复制草案

```text
原创漫剧的空场景主参考：{地点}，{昼夜/天气}。
空间布局固定为：{入口、窗、桌、通道和其他关键结构}。
机位在{具体位置}朝向{方向}，{景别}；前景{内容}，中景{内容}，后景{内容}。
主光来自{光源与方向}；材质与色调为{描述}；风格为{统一风格}。
不出现人物；不添加原设定没有的门窗；关键表演区域保持清晰。
最终构图面向{画幅}，输出尺寸按工具实际支持另设。
```

## 08｜角色参考 + 场景参考 → 分镜关键帧（manju-prompts）

```text
附件图1：角色身份和衣装参考。附件图2：场景布局参考。
只取图1的人物身份和服装，不带入图1背景；只取图2的场景结构和光线，不带入无关人物。

为镜头{镜号}生成一张静态关键帧。
画面中的确定瞬间是：{主体、姿势、正在发生的单一可见动作}。
景别{景别}，机位{角度}；主体位于画面{位置}，面朝{方向}，视线落向{对象}。
前中后景关系：{描述}；手中道具及左右：{描述}。
保留参考人物的{关键锚点}，保留场景的{关键布局}，光线按{场景参考/明确约束}。
沿用{确认风格}。不增加人物、饰品和门窗，不把动作过程画成多格漫画。
需要的对白、字幕或纸条文字留给后期，不画进底图。
```

## 09｜局部修改（manju-prompts）

```text
编辑附件中的底图，不重新设计整张画面。
修改区域：{明确位置，如“画面右侧人物的右手及其握住的杯子”}。
唯一修改目标：{例如“把杯子换为已提供道具参考中的旧表，手指形成自然握持”}。

必须保留：人物身份、脸部、发型、衣装、身体姿态、背景布局、镜头裁切、其他道具、整体光向。
仅允许目标物体附近必要的接触阴影和遮挡关系调整。
若工具支持蒙版，使用已提供蒙版指导修改；格式按该工具要求，不假设黑白含义通用。
不要顺便美化脸、换衣服、改变背景或添加文字。

完成后检查：改动目标是否正确；握持与遮挡是否自然；非目标区域是否有意外变化。
“保持不变”是验收目标，不是对模型能逐像素保留的保证。
```

## 10｜跨工具交接与版本回写

```text
这次我在 ChatGPT 确认了以下修改：{内容}。
请先比较本地 STATE.md 采用版本与我提供的版本，不把两份冲突稿直接合并。
只把已明确确认的部分写入新的版本，保留旧文件。
更新 STATE.md 和 CHANGELOG.md，并列出需要重新检查的镜头、视觉设定和提示词。
没有实际本地访问权限时，请给我建议文件名与完整内容，不声称已写回电脑。
```

## 使用边界
这些模板控制流程、信息结构与复核方式，不保证文学质量、角色一致性或商业回报。图像/视频模型不同，其参考输入、蒙版、尺寸、时长与参数支持也可能不同。通用提示词应先做小样，确认结果后再批量使用。


---
# v2｜制作与后期提示词（有工具才能执行）

## 20｜全流程统筹（manju-producer）
```text
读取项目规则、STATE、实际能力配置与当前采用产物。
只规划本轮的一批工作，写输入版本、输出、依赖、检查点和费用/上传范围。
把“文字计划已完成”“媒体已落地”“用户已确认”分开。
没有的适配器列为阻塞；不擅自安装、付费或声称后台运行。
上游变更时只标记真正受影响的下游，不全量重做。
```

## 21｜声音定妆与逐句配音（manju-voice）
```text
根据已确认人物资料、内容依据（A 的 source_packet / B 的剧本）和逐句台词表，给每个角色做声音定妆卡：
音色质感、音域、口音、语速倾向、允许的情绪变化、不可变化的身份特征。
先提出少量试音文本，不批量生成。
每句填写 line_id、角色ID、voice_version、spoken_text、direction、subtitle_text、读音词典。
选择真实服务后再填写 model/voice_id；没有权限或工具时为 null。
实际生成/导入后记录 take 文件、SHA-256、实测时长、费用已知值或 unknown。
不得把表演指导念进台词；不为凑镜长强行加速，不把配音完成当成口型同步完成。
```

## 22｜视频任务与采用区间（manju-video）
```text
每镜绑定确认关键帧、角色/服装版本和实际参考输入。
描述一个可完成的主要动作、镜头运动、起止状态。
区分计划采用时长、模型支持档位和返回实测时长。
先出单镜任务小样；没选模型不编造参数，没配置适配器不调用。
记录异步 job_id、候选 take、采用源入点、时长与连续性检查。
状态未知先查询，不盲目重新付费生成。
```

## 23｜字幕对齐（manju-subtitles）
```text
使用实际采用配音/定剪音轨和确认台词对齐，不按字数伪造时间戳。
输出时间线绝对毫秒 start_ms/end_ms、line_id、音频资产ID和字幕文本。
记录真实对齐方法、检查依据以及 timeline_timing_sha256。
ASR 错字不自动改写采用台词或内容依据；无音频/无对齐工具就列阻塞，保留 draft。
不把外挂或软字幕说成烧录字幕。
```

## 24｜音乐、音效与混音（manju-sound）
```text
按故事节拍设计对白、音乐、音效、环境四类声音。
给每个 cue 写用途、入点、出点、固定增益、淡入淡出和素材许可。
保留独立源轨，避免音乐压住台词。
只把有工具实测过的响度/峰值填成数值；未知目标和未测项填 null/not_checked。
基础混音不假称为自动ducking或母带处理，不自行下载未授权歌曲。
```

## 25｜可重建剪辑（manju-edit）
```text
根据当前采用素材和实际配音建立 timeline.json。
视频以整数帧定位，素材入点用毫秒，声音允许跨镜。
填写真实文件与哈希；缺素材先报告，不生成黑片冒充成片。
使用当前后端实际支持字段，不支持的效果显式报告并等我选替代。
先 validate 和 dry-run，不改原件、不覆盖输出，确认后再渲染低分辨率小样。
导出后报告实测帧数/时长和完整解码结果；剧情/视听质量需另外检查。
```

## 26｜交付检查（manju-delivery）
```text
检查实际成片的媒体流、完整解码、分辨率、帧率、时长、字幕与音轨。
能看/听时再检查画面、口型、道具、错字、同步与音乐压人声。
没有能力检查的项写 not_checked，不以文件存在替代通过。
核对素材/声音/音乐/字体使用条件与目标平台的当前发布要求。
只列真实交付文件，不自动发布，等我确认最终版本。
```

## 27｜关键帧生成与导入（manju-images）
```text
把确认视觉设定和提示词变成逐图任务，绑定真实参考文件与SHA-256。
先做角色/场景小样，不批量。接口未配置只准备任务，允许我手动生成后导入。
实际图像保存为新take，再检查可打开性、规格、人物/服装/道具/构图。
没看过图就标待目检，任务成功和用户批准分开记录。
```
