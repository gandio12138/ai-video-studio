# 中文提示词库｜v2.2

## 固定完整入口
有小说：读取`prompts/entry_novel_direct.md`并提供实际文件与片段。
没有小说：读取`prompts/entry_original_script.md`并给设定、人物、概述。
已有稿只缺视觉：读取`prompts/complete_visual_pack.md`。

两入口都读VISUAL_PRODUCTION.md并交详细视觉包，不把“不生图”误解为“不写提示词”。旧库留在docs/archive/PROMPT_LIBRARY_v21.md，仅作历史参考，冲突以当前项目规则为准。

## 单独补细节
```text
$manju-enrich
使用STATE中的当前内容，按VISUAL_PRODUCTION.md主动补服饰、外貌、场景、道具和局部事件接续。
列CMP台账，A/B直接写具体草案，C核心提案不要暗中采用。
不改原文/已确认稿；桥接写起点→补动作→终点，检查新知识与时长。
```

## 单独做详细美术
```text
$manju-visual
读取当前内容、CMP台账和镜头需求，写STYLE与详细人物/衣装/场景/道具卡。
解释设计为什么符合身份、财力、时代、任务与天气。
服装写层次/剪裁/材质/配色/鞋饰/旧损，场景固定布局/材质/光源/活动区。
非核心缺口主动填成提案，不只给标签。完成后列参考任务，先不生成。
```

## 把设计真正写成提示词
```text
$manju-prompts
按VISUAL_PRODUCTION.md完成当前集完整提示词。
每独立角色半身、全身、侧、背、关键表情各一条；每场景主、反/替代、关键区域各一条；关键道具、每个镜头、两类单一区域编辑预案全部覆盖。
正文展开可见设计，不只写编号/同上；缺参考写依赖与条件式完整稿，不谎称已上传。
输出真实文件索引和缺项，不执行生图。
```

## 修改而不重做全部
```text
$manju-producer
本次只修改：[实际CMP/人物/衣装/场景/镜头与新要求]。
比较采用版本，保留原稿与批准记录，列最小影响。
将草稿写新版本，同步详细卡/镜头/提示词；不随机改变无关人物或背景。
不因为只改衣服就重配整集声音，不自动执行任何生成或渲染。
```

## 后续配音
```text
$manju-voice
读取已确认的source_packet或原创剧本、同版逐句台词表和人物卡。
先做声音卡与小量试音计划，实际音色ID/授权/上传/费用未定不生成。
spoken_text与direction分开，真实音频返回后测时长，不冒充已录音或已对嘴。
```

## 后续视频与剪辑
```text
$manju-producer
沿用STATE采用的真实参考、音频和镜头版本。
先检查实际资产/服务/批准，再由video准备逐镜任务、edit建中立timeline。
缺接口就报告，不伪造生成；先检查与dry-run，获本地渲染授权再渲染。
时长/字幕用实际素材，保留原件、take、哈希与版本。
```

历史声音/视频/后期模板的合法边界仍适用；当前更完整技能文件位于.agents/skills。默认中文自然语言；模型专有参数接入时再核实。
