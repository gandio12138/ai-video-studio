# 视觉开发与叙事补全规范｜v2.2

这是本 harness 的共同创作协议。两个入口默认都要交付**细节补全、人物定妆、场景设计、文字分镜，以及可直接复制的人物/场景/逐镜生图提示词**。它不是模型参数、效果保证或媒体执行授权。

## 1. 适用顺序与完成边界

优先遵守用户本轮明确范围、已确认的硬设定和本项目规则。只初始化、只选片段、只改某一项时，不擅自扩大任务。原文不可读、片段未选时先解决对应问题，不虚构正文。

片段或首集创意明确、用户调用 v2.2 入口后，完整文字批次为：内容依据 → 补全与衔接 → 人物/场景文字设计 → 文字分镜草稿 → 图像提示词包 → 覆盖与连续性检查。各工种可回看并小范围修订，不能各自发明一套身份、服装和场景。

**“不生图”只禁止实际生成图片，不禁止写生图提示词。**缺 API、缺真实参考图、尚未 G1/G2 批准，不阻塞文字设计、试拆和条件式提示词；但不得称这些草稿已获批准或直接提交媒体任务。

## 2. 主动补全，不把空白全部退给用户

先保留事实和硬约束，再为画面与故事接续补全。使用本轮入口即授权下面 A/B 类的**文字草案补全**，不必逐项询问。补全结果先写入补全台账，再被人物、场景、镜头和提示词共同引用。不是每镜临时随机设计。

| 类别 | 主动处理范围 | 边界 |
|---|---|---|
| A：视觉制作细节 | 年龄观感、轮廓、发型、体态、服装层次/剪裁/材质/颜色/磨损、随身物品、建筑/家具/天气/光线/陈设 | 不覆盖原文或用户已给定外貌；不借美化改变身份、经济条件、时代和关键线索 |
| B：局部表演与衔接 | 进门后走到哪里、衣摆如何收拢、视线转向、伸手/接物/放物、合理的小停顿、场景切换的建立镜头、必要无对白背景人 | 连接已存在的起止状态，不创造新知情者/关键证据/能力/危险/结局；新增动作计入时长，不能硬塞 |
| C：核心创作或改动 | 新主要人物、新亲属或仇敌、改变身份/目标/动机/结局、增加关键道具功能、支线、反转、重要信息获取事件、解释性对白 | 单列核心提案，最多两个方案。novel_direct 未确认不混入主版本；original_script 可按明确标注的临时方案试写，但不能覆盖用户硬设定 |

original_script 的未给定剧情可以在用户方向内主动创作；不要把所有新动作或新事件都当作必须先问的 C 类阻塞。涉及核心空白时记录临时方案，涉及用户已确认硬设定的改变则先等批准。

B 类补充的人物只有确实需要时才加，例如原文已发生点餐，可让未命名店员完成递碗；先说明叙事功能和制作成本。不能为了“丰富画面”硬加群众、独立角色支线或一段新对话。新增人物不得替代主角完成关键选择。

**事件补全必须形成“已有起点 → 补充动作 → 已有终点”**。让人物从门口走到柜台，通常可在 B 类草稿中完成；凭空加一次偷听让他得知真相，属于 C 类。原文故意省略的秘密、有意悬念、未来反转不能被当漏洞提前解释。

心理描写可用不改变事件含义的表情、呼吸、握力、回避视线等表演表达；不能把“他怀疑被跟踪”直接补成实际尾随者。原文逻辑自身冲突先报告；不是补全许可下的自动修复。

## 3. 事实与补充台账

使用 `templates/completion_ledger.md`，给补充稳定编号 `CMP001` 等。字段：原始缺口；依据/读取范围；来源类别；补充类别 A/B/C；具体方案；为何符合人物与世界；影响的角色/场景/道具/镜头；进入/退出状态；潜在因果与成本变化；状态；用户采用依据。

来源类别区分：`source_fact` 原文事实，`user_given` 用户给定，`inference` 推断，`visual_proposal` 视觉设计，`bridge_proposal` 衔接补充，`core_proposal` 核心提案。没有原文出处就写“本轮制作补充”，绝不捏造章段。补充前后的用户确认状态分开记录。

A/B 可按批次许可写入 draft，不每条停下；C 按上表处理。已有 approved 内容的变更先列影响并等确认。后读章节出现冲突时回查台账、生成新版本，只让相关资产/镜头/提示词 stale，不能反改原文使其迁就设计。

## 4. 人物：美感来自符合身份的设计，不是统一美颜

每个本轮主要或需要独立识别的出镜人物，使用 `templates/character_design.md`。必须写实质内容：

- **人物依据**：年龄/时代/身份/经济状态/经历/此时目标/性格表现/身体状态；每条区分已知与提案。
- **辨识与比例**：年龄观感、体型和肩背姿态、脸部轮廓、眉眼和鼻口关系、发色/发型/发际线、必要的痣/疤/标记。选择少量显著锚点；没有叙事需要不堆疤痕和神秘纹身。不要把所有人物都设计成同一张年轻脸。
- **服装完整系统**：内搭、外层、领口/袖型/衣长/开合方式、腰部固定、下装、鞋；能看见的内层说清。明确主色/辅色/点缀色、布料或皮革的厚薄/垂坠/反光、缝线/包边/修补/磨损、适合动作的结构。
- **人物与衣装关系**：为什么此人会穿这套；身份、财力、气候、任务是否支持；谨慎/张扬通过如何穿戴和保养表达，不把性格直接等同某种脸型或肤色。
- **配饰与道具**：用途、数量、大小参照、位置（人物自身左右）、携带与取用方式；不凭装饰添加家族纹章或新魔法规则。
- **状态版本**：`C001_OUTFIT_A` 基础衣装，`C001_OUTFIT_A_WET` 雨湿派生状态；受伤/破损/换装有事件来源。身份、衣装、状态、动作分开锁定。

美感检查：轮廓是否清晰；主次是否集中；配色是否协调且服务人物；材质是否有区别；局部设计是否可信。贫困人物也能有克制、整洁、协调的美感，不必镶金；年长人物不必被改成少女。保留身体与年龄特征，不用露肤、白皙或夸张身材替代设计。

### 人物必须交付的文字提示词

每个独立识别角色：
1. 单人半身主定妆（看清身份，无复杂叙事背景）；
2. 单人全身正面（头发、鞋和衣装轮廓完整）；
3. 单人侧面；
4. 单人背面；
5. 本集最重要的一种表情/姿态。

每项是独立可复制提示词，不要求一次生成五张或拼成多格。侧背/表情任务以已确认主参考为条件，参考尚未生成时状态 `awaiting_reference`，仍先写好完整条件式文本。远景群众可用群体设计条目，不强制给每人五张；必须列明例外理由。第二套衣装仅在本集确实用到时设计。

## 5. 场景：可表演的空间，不是装饰背景

每个本轮场景使用 `templates/scene_design.md`。明确地理/文化与时代（架空不可装成史实）、场所功能、主人经济条件、尺度参照、结构与材质、维护程度、关键陈设与使用痕迹。

先给固定布局和坐标约定：门、窗、柜台/桌、通道、主要道具与人物站位，用平面文字图或方向关系表达。**世界坐标固定，画面左右随机位改变**；反打不能把房间重新设计成镜像房间。给主机位、反向机位与轴线，指明能看到/看不到什么。

光线写具体来源、方向、相对软硬、明暗和冷暖层次；天气/雾/尘/雨痕需符合空间状态，不为“氛围”让室内无故大雨或满屏粒子。场景卡可写光源布局；每张提示词还必须从该机位表述画面中的入射方向。

构图要有主要视觉焦点、前中后景、引导线、人物活动区与负空间；背景复杂度为对白、动作和线索让位。重要线索不要藏在难以辨识的远景。空场景不放人物，但留下可信的生活/工作痕迹。

### 场景必须交付的文字提示词

每地点至少三项：空场景主视角、同一空间反向/替代机位、剧情关键区域细节。三者共用布局/材质/光源/时段版本。日夜或事件后状态只有用到时才增加。关键道具另外给独立提示词；纸条/招牌/屏幕中必须准确的文字列为后期叠字，不算图像底稿已经可靠完成。

## 6. 全集统一美术方向

默认先给一个与题材/人物/预算匹配的推荐草案，不把“没选画风”作为全部文字工作的阻塞。沿用已确认画风；没有时用可操作语言说明：人物比例、线条粗细与边缘、阴影方式、颜色饱和度、材质细节密度、背景复杂度、光线与构图倾向。记录为 `STYLE_v01`，解释它与故事的关系。

不要把“精致、绝美、大师级、8K、电影感”当作主体内容。可以有评价词，但必须对应看得见的设计决策。不要为了细节堆叠不相干花纹/首饰/书架/粒子。避免一条提示词混用互相排斥的写实照片、平面儿童画、厚涂3D等要求。

角色定妆用于读清身份，场景图用于确认布局，故事关键帧用于叙事；三者可共用美术风格，但不能都用遮脸强逆光或时装海报姿势。

## 7. 每镜提示词：完整展开，不让另一个模型猜

每镜至少一条独立静态关键帧提示词，双人对话按各自身份、站位与视线明确区分。跨状态或确需起止帧的镜头可增加，非必要不翻倍。

可复制正文必须包含：画面用途/画幅与固定瞬间；画中可见人物的身份外观与衣装关键锚点；人物姿势、哪只手拿什么、朝向和视线；场景可见布局和材质；景别、机位、前中后景与主体大小；主光来源、方向、冷暖和层次；统一美术描述；必要约束与后期。

- **不能只写“C001 + L001 + STYLE_v01”或“服装同上”**。编号留在任务元数据，复制正文展开必要描述；特写只展开可见手/袖口/道具，不塞满看不见的鞋和后背。
- 静态图只选一个明确瞬间；走进→落座→递信等过程属于分镜/视频动作，不能要求一张静帧同时发生。
- 配角可以减少细节，但不是主角模板换名字；两个角色的服装、轮廓和颜色需要区分且符合设定。
- 将参考输入表、来源/CMP编号、正向正文、约束、后期、验收分开。正文不用携带冗长管理标签。
- 参考只有真实提供时才写实际路径；计划参考填 null 和所依赖任务，不假装已上传。初次主定妆/空景可无参考；派生稿写清身份/服装/布局/风格分别取哪张图。文件名/编号不是视觉记忆。
- 模型未知时给通用中文自然语言，不编造 `--ar`、seed、权重、蒙版含义或负面参数。尺寸在自然语言中是构图意图，不代表某接口已支持。

## 8. 局部修改提示词

首批至少预写两类条件式修正任务：人物衣装/手与道具的单一区域修正；场景布局中一个区域的修正。写清底图需要什么、修改区、唯一目标、保留项、可允许的接触阴影与遮挡调整、验收。未见结果时这是预案，不能声称已发现图中错误。衣装修改不能顺便换脸，场景局部修正不能重排全部门窗。真实工具的蒙版规则接入时另核实；不承诺逐像素锁定。

## 9. 强制交付清单与覆盖检查

当前片段/首集完整文字批次须生成：

| 产物 | 建议路径 | 完成含义 |
|---|---|---|
| 原始事实/用户设定与补全台账 | `01_story/EP01_completion_ledger_vNN.md` | A/B细节写实、C待决定，来源与作用可追溯 |
| 事件/动作/状态接续表 | `03_shots/EP01_continuity_vNN.md` | 谁从哪到哪、拿什么、何时知道什么、前后镜接得上 |
| 全集美术方向 | `04_visual/STYLE_vNN.md` | 具体风格、配色、细节层次与理由 |
| 人物/场景/道具卡 | `04_visual/characters/`、`locations/`、`props/` | 每个必要资产有详细设计，不是一行标签 |
| 文字分镜 JSON 与 MD | `03_shots/EP01_shots_vNN.*` | 镜号、景别、动作、台词、时长、素材、来源/补全与连续性 |
| 人物提示词 | `05_prompts/characters/C001_vNN.md` | 每角色五类独立正文或有依据的群众例外 |
| 场景提示词 | `05_prompts/locations/L001_vNN.md` | 每场景三类独立正文 |
| 关键道具提示词 | `05_prompts/props/P001_vNN.md` | 形状/比例/材质/状态明确；没有关键道具则标不适用 |
| 逐镜关键帧提示词 | `05_prompts/shots/EP01_SH001_vNN.md` | 本轮镜头全覆盖，不能仅示范头两镜 |
| 局部编辑预案 | `05_prompts/edits/EP01_edit_recipes_vNN.md` | 至少两种明确的条件式修正任务 |
| 总索引 | `05_prompts/EP01_visual_pack_index_vNN.md` | 真正输出文件、任务条目、参考依赖、数量、草稿状态与缺项 |
| 验收报告 | `07_reviews/EP01_visual_continuity_review_vNN.md` | 覆盖、原文/设定一致性、审美具体性、时长、未执行项 |

范围小可以把同类提示词合并成一个 MD，但每条要有稳定标题/任务 ID、完整正文与索引定位。不能只有“应当生成哪些提示词”的计划、空模板或未填括号。用户输入处的占位符正常；给用户的正式草案中普通外观和场景空白应当已被填成提案。

交付前逐项检查：必要角色是否全覆盖？每场景是否三个视角？每镜是否有正文而非占位？所有人物/衣装/场景版本一致吗？核心补充是否偷偷采用？反打左右是否合逻辑？加入动作是否计入时长？重要细节能在该景别被看见吗？审美词是否转成具体造型/构图/材质/灯光？

内容过多时按当前集分批真实落盘，报告已完成和仍缺条目，不能用“后续再补提示词”宣称整包完成。不要擅自扩大到整本小说或全季。

## 10. 状态、批准与后续生产

设计文档/分镜为 `draft`，提示词制作状态为 `prompt_only`，缺参考的派生任务另记 `awaiting_reference`。这些不是图像 job 已成功。实际素材未产生时 `assets.json` 保持 planned / path=null。

G1 锁内容依据、原文范围或剧本、采用台词与叙事补充；G2 锁人物/衣装/场景方案及声音身份。定妆小样可在用户明确的小样授权后执行，派生/镜头任务须依实际参考与对应批准推进，不以文件存在自动授权。

配音、视频、字幕、混音、剪辑仍用原有数据与技能。修改视觉细节不自动重配所有声音；若改变台词/动作时长，再追踪相关声音、字幕和时间线。此规范不新增生成接口，也不保证模型输出审美或一致性；实际图片必须另做目检。
