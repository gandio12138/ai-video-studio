# 两种创作入口｜v2.1

这是项目的入口与阶段选择规则，不是新模型、可执行调度器或额外媒体权限。保留原有十四个 skill；配音、视频、剪辑、字幕、混音与交付共享同一套后半程。

## 1. 选入口，而不是先问有没有小说

| entry_mode | 适用输入 | 本轮起点 | 不应强制执行 |
|---|---|---|---|
| `novel_direct` | 实际提供的小说正文或可读文件，用户要求不打磨剧本 | 原文范围核对 → 制作依据与台词提取 → 文字分镜试拆 | 原创选题、重写大纲、独立场景剧本、对白润色 |
| `original_script` | 用户的设定、人物描述、故事概述 | 创意简报与人物目标 → 首集大纲 → 剧本试写与审校 | 索要小说、生成 EPUB、先写小说正文 |
| `undecided` | 尚未选择 | 只问需要哪种入口 | 仅凭目录里存在 EPUB 就替用户选模式 |

完整入口提示词：
- `prompts/entry_novel_direct.md`
- `prompts/entry_original_script.md`

`PROJECT.md` 的 `entry_mode` 是入口采用值；`STATE.md` 记录本轮范围、阶段、文件与用户确认。首次明确调用一个入口即可记录该选择，但不等于批准后续创作。两处冲突时先核对。

若旧项目已有内容或媒体，切换入口/故事须先列影响并征求确认。不同故事优先用平行项目目录，不能清空或复用旧人物设定。只初始化时不自动选路或创作。

## 2. A 路线：有小说，不再打磨剧本

```text
只读原文 → 指定章节/片段 → 人物与场景事实提取
                   ↓
      原文制作依据（source_packet）＋逐句台词表
                   ↓
       文字分镜试拆＋原文忠实度/可拍性检查
                   ↓
      G1：用户确认范围、制作依据、采用台词与补充
```

跳过的是独立编剧、润色和创作性改写，不是跳过来源核对、台词提取或制作整理。

允许：分场、分镜、提取已有对白、把环境描写整理为画面描述、给镜头与台词编号、估时；有声叙述只从实际提供且选中的原文提取。未明确的衣装、机位、表演等写成【制作提案】，不能冒充原文事实。不能把未在原文发生的事件作为“心理可视化”偷偷加进故事。

默认不允许：改身份/关系/动机/事件结果；补支线、反转、解释性对白；压缩、口语化或重写台词；为了时长删掉关键因果。原文问题可以记录，不能擅自修复原作。

读音、引号和排版清理须可追溯，不改变说话内容；实质改词、合并对白、删减信息均先提案。心理段落不能等价拍出时标待定，可建议保留原文旁白，不自动编对白。时长装不下时优先建议延长或拆集，需用户决定删改时暂停相关部分。

输入是 EPUB 时先检查已有提取工具。当前包没有专用 EPUB 导入器；不能宣称已内置。无可读正文时报告缺项，可以请求用户提供 TXT，或在得到允许后补本地导入器。未成功读取不能凭对书的记忆替代。未指定片段时只检查索引/开头有限范围，给最多两个片段候选并停止；不能整本直接批量拆镜。

此路线由 producer 统筹，story 仅提取原文事实，shots 负责制作拆分，audit 只检查忠实度、连续性和可拍性。默认不调用 script 润色。没有独立场景剧本不构成阻塞。

## 3. B 路线：没有小说，从原创剧本开始

```text
用户设定＋人物＋概述 → 原始创意记录与设定简报
                               ↓
           人物目标＋首集大纲 → 剧本试写＋剧情检查
                               ↓
                  G1：用户确认剧本和逐句台词
                               ↓
                       文字分镜
```

用户已给方向就直接发展，不反复要求重新选题。保留用户原话，区分【用户给定】【AI补充提案】【待决定】。非核心空白可补充并集中列出；改变核心身份、关系、世界规则、结局的空白最多给两个方案，有推荐时可按明确标注的临时方案试写，不自动定稿。真正阻碍写作才提问，一次最多三个。

首轮允许在同一个批次完成简报、人物、首集大纲、首集剧本试写与检查；不必每个小文件都停下来问。全部为 draft，批次授权不等于 G1 批准。默认只试写第一集，不写小说正文或全季。后续按用户反馈打磨并确认，再交 shots。

已有大纲/剧本获批时从实际进度继续，不重跑第一稿。旧小说、examples 和未指定文件不作为原创事实来源。

## 4. 两条路线共用的制作依据与台词接口

| 项目 | novel_direct | original_script |
|---|---|---|
| G1 内容文件 | `02_scripts/EP01_source_packet_vNN.md` | `02_scripts/EP01_vNN.md` |
| 文件含义 | 原文范围、事实、分场与声音提取，**不是改写剧本** | 原创场景剧本 |
| 逐句台词表 | `02_scripts/EP01_lines_vNN.json` | 同左 |
| 人物资料 | 原文已知身份/关系/行动；未知不补剧情 | 用户设定＋明确标注的补充提案 |
| 审校重点 | 原文忠实度、连续性、时长与可拍性 | 因果、动机、信息与对白质量 |

台词表用 `templates/line_sheet.json`，统一 `line_id`、`speaker_id`、`spoken_text`、`direction`、`subtitle_text`、`source_refs`。对话和旁白都有稳定台词编号，跨镜声音只引用一次音轨；非出镜旁白可登记为声音角色，不能凭旁白身份增加画面中的人。

沿用 `templates/tts_job.json` 的 `script_ref` 字段名以兼容 v2；它表示 G1 的内容依据：A 可引用 source_packet，B 引用 script。不要求 A 生成空剧本占位。TTS job 的 input_refs 还应包含本轮采用台词表及声音卡，填真实文件哈希。任务单未执行不等于已有声音。

G1 在两条路都确认内容依据、台词、范围、关键补充。A 的入口提示词明确授权在 G1 前试拆文字分镜；只能标 draft，不得拿试拆稿直接付费生成。B 首轮默认在剧本审校后停止。

G1 后共享：文字分镜复核 → 视觉/声音定妆（G2）→ 实测配音与关键帧 → 有声小样/批量预算（G3）→ 动态镜头 → 粗剪/定剪（G4）→ 字幕/混音/交付确认（G5）。视频镜长不反向授权改台词。更改采用文字后仅标记受影响的 TTS/字幕/镜头/时间线。

## 5. 共同边界

首轮仅文本与离线检查，不生图、不配音、不生成视频、不渲染、不安装依赖、不调用付费接口。运行模式与预算按原有 config 与批准机制另行管理，入口选择不改变权限。

保留原稿与已批准版本，输出同名递增；不伪造读取、用户批准、授权、素材路径、服务能力或实测时长。缺小说仅阻塞 A 的原文读取，不阻塞 B；缺云服务不阻塞文字开发。记录“已提取”“模型已阅读”“用户已确认”三种不同状态。

这是 agent 应遵循的工作约定；本次更新没有实现 EPUB 解析、媒体 API 或自动批准执行器。实际能力见 CAPABILITIES.md。
