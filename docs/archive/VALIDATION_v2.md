# v2 验证记录｜2026-09-10

## 实际执行
环境：本次Linux x86_64容器，Python3.13.5；FFmpeg/ffprobe7.1.5。没有访问用户Mac，没有执行Codex CLI，没有调用任何付费API。
`python3 -m unittest discover -s tests -v`：56项通过。原镜头表检查16项，新增40项；明细见tests/unit_test_results.txt。
`python3 tools/smoke_test.py --root /mnt/data/_manju_v2_smoke`：通过。测试目录不打包，生成器与汇总随包提供。

## 实际渲染测试
使用合成纯色/测试图案、两个正弦测试音，不是真人声音、TTS或正式故事素材。
输出4秒、120帧、180×320预览，测试外挂字幕、软字幕、无音频的显式静音三种情况。
检查：dry-run不生成媒体；源视频入点裁切/硬切；音频裁切、定位、淡变和多轨；外挂字幕复制；软字幕流；完整解码、分辨率/帧数/时长；不覆盖已有版本。还检查切点前后实际解码画面变化及计划入点后的音量上升。
汇总见tests/reports/smoke_summary.json。没有进行语言识别/配音质量/主观画面审美验证。

## 没有验证的内容
你的macOS/FFmpeg构建与Codex技能发现；云端模型/API/价格/账户权限；普通话配音情绪和稳定性；自动ASR/口型；NLE原生工程；任意外部媒体兼容性；长片或复杂混音；商业内容权利；发布平台终审。相关状态保持未接入/未检查。

## 校验边界
结构验证不是完整JSON Schema引擎，也不是逐任务程序调度器/账单汇总服务。任务审批字段和SHA-256用于协作与变更追踪，不是防恶意篡改的数字签名或操作系统安全边界。工具成功只说明对应测试通过，不能由此宣布已自动做出可发布漫剧。
