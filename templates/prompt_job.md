# 图像任务单
任务编号：；关联镜头/资产：；版本：；状态：prompt_only。
任务类型：角色定妆 / 场景空镜 / 道具 / 参考合成 / 分镜静帧 / 局部修改。
目标工具与具体模型：未指定时写“通用自然语言草案”；不要编造接口能力。

## 参考输入清单
| 顺序/标签 | 实际路径或附件 | 用途（身份/服装/布局/风格/底图） | 已上传/尚缺 | 确认状态 |
|---|---|---|---|---|
尚缺参考图时可写条件式草案，但不可声称参考已生效。

## 正向提示词（可复制）
画面用途与主体 → 固定身份/外观 → 可见姿势与动作 → 场景 → 景别/机位/构图 → 光线与风格 → 明确保留项 → 输出要求。
这是静态图时只描述一个确定瞬间；动态过程另写视频动作提示词。

## 约束与避免项
人物、服装、背景、人数、位置、文字等本任务相关限制。不要默认每个工具都有 negative_prompt 字段。

## 局部编辑（仅编辑任务）
基底图：
编辑区域与定位：
唯一修改目标：
必须保持不变：
允许的局部光影过渡：
蒙版：已有/待制作/工具未确认；格式要求按目标工具另核实。

## 构图与后期
最终画幅、主体画面位置、前中后景、视线方向、字幕/界面安全区（平台未定时标临时）。
不得把自然语言画幅要求当作 API 已支持某一像素尺寸。

## 参数适配
仅在具体模型文档已核实后写受支持的参数；否则留空。

## 验收
身份/服装/场景是否与参考一致；动作可读性；重要细节；局部编辑是否波及非目标区域；文件是否真实保存。
